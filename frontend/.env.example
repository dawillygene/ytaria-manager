# Web (served by Nginx on the same origin as the API): leave empty.
# Android production build: the public HTTPS origin of your deployment.
VITE_API_BASE_URL=
# Android emulator debug builds reach the host machine at 10.0.2.2, e.g.:
# VITE_API_BASE_URL=http://10.0.2.2:8000
